CREATE SCHEMA IF NOT EXISTS SCHOOL;




GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA SCHOOL TO postgres;



CREATE TABLE SCHOOL.STUDENT ( ID    INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, NAME   VARCHAR(40) NOT NULL CHECK (NAME <> ''), 
EMAIL   VARCHAR(40) NOT NULL CHECK (EMAIL <> ''));
